# Optional — fast command parsing (https://console.groq.com/keys)
GROQ_API_KEY=

# Optional — smarter command parsing + movie guessing fallback
GEMINI_API_KEY=

# Optional — web search for where movies stream (https://brave.com/search/api/)
BRAVE_API_KEY=

# Optional — accurate streaming availability lookup (https://www.themoviedb.org/settings/api)
TMDB_API_KEY=