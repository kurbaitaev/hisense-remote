# Recommended — voice transcription + smarter command parsing
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=

# Optional — alternative command parsing (not required for search-only mode)
GEMINI_API_KEY=

# Optional — not used by voice search
BRAVE_API_KEY=
TMDB_API_KEY=